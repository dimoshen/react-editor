import { FC } from 'react';
import './Header.scss';

export const Header: FC = () => (
  <header className="Header">
     <div className="Header__title">
      React Editor
     </div>
  </header>
);
